const filterGigsByUser = function (gigs, user) {
    // check Config Vars for user email
    // if so, filter gigs using key in Config Vars
    // return filtered gigs
    // set filtered gigs to state
};